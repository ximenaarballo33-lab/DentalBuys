{
  "name": "dental-buys",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.95.0"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.1.3"
  },
  "scripts": {
    "build": "echo \"Ready for deploy\"",
    "deploy": "wrangler deploy",
    "publish": "wrangler pages publish . --project-name dental-buys"
  }
}
